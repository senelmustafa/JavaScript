import React from 'react'

const SectionHeader = () => {
    return (
        <div className="section-header">
            <h3>{title}</h3>
            <p>{desc}</p>
            <span>{title}</span>
        </div>
    )
}

export default SectionHeader